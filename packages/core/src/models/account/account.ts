/**
 * Account type.
 * @example
 * ```typescript
 * const account: Account = {
 *     name: "my-account",
 *     privateKey: "0x1234567890abcdef",
 * };
 * ```
 */
export type Account = {
    name: string;
    privateKey: string;
};
